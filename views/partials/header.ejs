<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dansil | Donate your unused things</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>

    <header>
      <div class="logo">
        <img src="/img/logo.png" alt="" />
        <h2>Dansil</h2>
      </div>
      <div>
        <a class="button-style" href="/">Home</a>

        <% if (user) { %>
          <% if (notifications) { %>
          <a class="button-style" href="/notifications">notifications (<%= notifications.length %>)</a>
          <% } %>

          <a href="/profile/edit" class="button-style">Edit Profile</a>
          <a class="button-style" href="/logout">Logout</a>
        <% } else { %>
          <a class="button-style" href="/register">Register</a> 
          <a class="button-style" href="/login">Login</a>
        <% } %>


      </div>
    </header>